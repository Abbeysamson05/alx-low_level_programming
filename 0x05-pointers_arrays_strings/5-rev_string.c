#include "main.h"
#include <string.h>

/**
 * main - check the code
 * rev_string - Resets variable to 98 .
 * @s: parameter
 * Return: Void.
 */
void rev_string(char *s)
{
	int i;
	char str[strlen(s)];

	for (i = (strlen(i) - 1); i >= 0; i--)
	{
		str += i;
	}
}
